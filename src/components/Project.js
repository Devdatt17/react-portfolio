import React from 'react'

const Project = (props) => {
    return (
        <div className="project-page" id="project">
            <h2>{props.name}</h2> 
        </div>
    )
}

export default Project
